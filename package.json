{
  "name": "nightshift",
  "version": "0.1.0",
  "description": "Autonomous queue of coding agents with its own memory. Ships the Claude Code plugin and the nightshift configuration CLI.",
  "license": "BUSL-1.1",
  "private": false,
  "bin": { "nightshift": "./bin/nightshift.mjs" },
  "files": [".claude-plugin", "bin", "plugin", "src", "README.md", "LICENSE"],
  "scripts": { "test": "node --test --disable-warning=ExperimentalWarning" },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  }
}
