{
  "name": "nightshift",
  "version": "0.1.0",
  "description": "Autonomous queue of coding agents with its own memory. v0 ships the Claude Code plugin only.",
  "license": "BUSL-1.1",
  "private": false,
  "bin": { "shift": "./bin/shift.mjs" },
  "files": ["bin", "plugin", "README.md", "LICENSE"],
  "engines": { "node": ">=20" }
}
