#!/usr/bin/env node
// Placeholder entry point: the nightshift runtime (queue, memory, hooks) does not exist in this repo yet.
process.stderr.write(
  'nightshift: not implemented yet — this repo currently ships only the Claude Code plugin in `plugin/`. See README.md.\n',
);
process.exit(1);
